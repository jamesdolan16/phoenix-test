{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "mix_task",
        "request": "launch",
        "name": "Debug Phoenix Server",
        "task": "phx.server",
        "projectDir": "${workspaceFolder}",
        "env": {
          "MIX_ENV": "dev"
        },
        "cwd": "${workspaceFolder}",
        "debugAutoInterpretAllModules": true
      }
    ]
  }